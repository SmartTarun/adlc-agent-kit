# Agent: Arjun | Sprint: 01 | Date: 2026-03-14
# Team Panchayat — Docker Compose Cluster
# Usage: docker compose up --build

version: '3.9'

# ── Shared workspace volume ───────────────────────────────────────────────────
volumes:
  workspace_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${PWD}
  postgres_data:
    driver: local

networks:
  panchayat:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

# ── Reusable agent config ─────────────────────────────────────────────────────
x-agent-base: &agent-base
  build:
    context: .
    dockerfile: Dockerfile.base
  env_file: .env
  volumes:
    - .:/workspace:rw
  networks:
    - panchayat
  restart: on-failure
  environment:
    - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
    - WORKSPACE=/workspace
  depends_on:
    dashboard:
      condition: service_healthy
  healthcheck:
    test: >
      sh -c "node -e \"
        const fs=require('fs');
        const s=JSON.parse(fs.readFileSync('/workspace/agent-status.json','utf8'));
        const a=s[process.env.AGENT_NAME||'arjun'];
        if(!a||!a.updated) process.exit(1);
        const age=(Date.now()-new Date(a.updated).getTime())/1000;
        if(age>300) process.exit(1);
        process.exit(0);
      \""
    interval: 30s
    timeout: 10s
    start_period: 120s
    retries: 3

services:

  # ── PostgreSQL ────────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: panchayat-postgres
    restart: unless-stopped
    networks:
      - panchayat
    ports:
      - "5432:5432"
    env_file: .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-adlc}
      POSTGRES_USER: ${POSTGRES_USER:-adlc_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-adlc_dev_pass}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-adlc_user} -d ${POSTGRES_DB:-adlc}"]
      interval: 10s
      timeout: 5s
      start_period: 20s
      retries: 5
    labels:
      - "com.panchayat.role=database"
      - "com.panchayat.sprint=01"

  # ── Dashboard ────────────────────────────────────────────────────────────────
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    container_name: panchayat-dashboard
    ports:
      - "${DASHBOARD_PORT:-3000}:3000"
    volumes:
      - .:/workspace:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - panchayat
    restart: unless-stopped
    env_file: .env
    environment:
      - WORKSPACE=/workspace
      - COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME:-panchayat}
      - PORT=3000
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 15s
      timeout: 5s
      start_period: 10s
      retries: 3
    labels:
      - "com.panchayat.role=dashboard"
      - "com.panchayat.sprint=01"

  # ── Arjun — Orchestrator (Claude Opus) ───────────────────────────────────────
  arjun:
    <<: *agent-base
    container_name: panchayat-arjun
    environment:
      - AGENT_NAME=arjun
      - CLAUDE_MODEL=${ARJUN_MODEL:-claude-opus-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    labels:
      - "com.panchayat.agent=arjun"
      - "com.panchayat.role=orchestrator"
      - "com.panchayat.sprint=01"

  # ── Vikram — Cloud Architect ──────────────────────────────────────────────────
  vikram:
    <<: *agent-base
    container_name: panchayat-vikram
    environment:
      - AGENT_NAME=vikram
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    labels:
      - "com.panchayat.agent=vikram"
      - "com.panchayat.role=infra"
      - "com.panchayat.sprint=01"

  # ── Rasool — Database Agent ───────────────────────────────────────────────────
  rasool:
    <<: *agent-base
    container_name: panchayat-rasool
    depends_on:
      dashboard:
        condition: service_healthy
      postgres:
        condition: service_healthy
    environment:
      - AGENT_NAME=rasool
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - POSTGRES_DB=${POSTGRES_DB:-adlc}
      - POSTGRES_USER=${POSTGRES_USER:-adlc_user}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-adlc_dev_pass}
    labels:
      - "com.panchayat.agent=rasool"
      - "com.panchayat.role=database"
      - "com.panchayat.sprint=01"

  # ── Kavya — UX Designer ───────────────────────────────────────────────────────
  kavya:
    <<: *agent-base
    container_name: panchayat-kavya
    environment:
      - AGENT_NAME=kavya
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    labels:
      - "com.panchayat.agent=kavya"
      - "com.panchayat.role=ux"
      - "com.panchayat.sprint=01"

  # ── Kiran — Backend Engineer ──────────────────────────────────────────────────
  kiran:
    <<: *agent-base
    container_name: panchayat-kiran
    environment:
      - AGENT_NAME=kiran
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    labels:
      - "com.panchayat.agent=kiran"
      - "com.panchayat.role=backend"
      - "com.panchayat.sprint=01"

  # ── Rohan — Frontend Engineer ─────────────────────────────────────────────────
  rohan:
    <<: *agent-base
    container_name: panchayat-rohan
    environment:
      - AGENT_NAME=rohan
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    labels:
      - "com.panchayat.agent=rohan"
      - "com.panchayat.role=frontend"
      - "com.panchayat.sprint=01"

  # ── Keerthi — QA Agent (starts in standby, activated by Arjun) ───────────────
  keerthi:
    <<: *agent-base
    container_name: panchayat-keerthi
    environment:
      - AGENT_NAME=keerthi
      - CLAUDE_MODEL=${AGENT_MODEL:-claude-sonnet-4-5}
      - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
      - WORKSPACE=/workspace
    restart: "no"
    profiles:
      - qa
    labels:
      - "com.panchayat.agent=keerthi"
      - "com.panchayat.role=qa"
      - "com.panchayat.sprint=01"
